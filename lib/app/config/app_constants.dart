import 'package:flutter/widgets.dart';

class AppConstants {
  static const double tPadding = 70;
  static const double bPadding = 20;
  static const double hPadding = 32;

  static const bodyPadding = EdgeInsets.only(
    top: tPadding,
    bottom: bPadding,
    left: hPadding,
    right: hPadding,
  );
}
